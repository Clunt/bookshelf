<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Library</title>
</head>
<body>
  <h1>Library</h1>
  <ul>
  <% for(var i = 0; i < books.length; i++) {%>
    <li>
      <%= books[i] %>
      <form action="/" method="POST">
        <input type="hidden" name="id" value="<%= books[i]['_id'] %>">
        <input type="submit" value="添加至书架">
      </form>
      <a href="/books/<%= books[i]['_id'] %>" target="_blank">Basic</a>
      <a href="/api/v1/books/<%= books[i]['_id'] %>" target="_blank">Api</a>
      <a href="/books/<%= books[i]['_id'] %>/update" target="_blank">Update</a>
      <br><br>
    </li>
  <% } %>
  </ul>
</body>
</html>